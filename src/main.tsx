import React from 'react'
import ReactDOM from 'react-dom/client'
// @ts-ignore
import FinalBusinessReport from './FinalBusinessReport.jsx';
import './index.css' // 如果有的話

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <FinalBusinessReport />
  </React.StrictMode>,
)
