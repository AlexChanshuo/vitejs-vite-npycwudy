import React from 'react'
import ReactDOM from 'react-dom/client'
import FinalBusinessReport from './FinalBusinessReport.tsx'
import './index.css' // 如果有的話

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <FinalBusinessReport />
  </React.StrictMode>,
)
